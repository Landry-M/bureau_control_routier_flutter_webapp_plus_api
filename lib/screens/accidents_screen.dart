import 'package:flutter/material.dart';

class AccidentsScreen extends StatelessWidget {
  const AccidentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Accidents')),
      body: const Center(child: Text('AccidentsScreen (stub)')),
    );
  }
}
