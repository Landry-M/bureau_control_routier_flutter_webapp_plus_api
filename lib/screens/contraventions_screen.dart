import 'package:flutter/material.dart';

class ContraventionsScreen extends StatelessWidget {
  const ContraventionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Contraventions')),
      body: const Center(child: Text('ContraventionsScreen (stub)')),
    );
  }
}
