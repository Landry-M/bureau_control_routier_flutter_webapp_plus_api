import 'package:flutter/material.dart';

class VehiculesScreen extends StatelessWidget {
  const VehiculesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Véhicules')),
      body: const Center(child: Text('VehiculesScreen (stub)')),
    );
  }
}
