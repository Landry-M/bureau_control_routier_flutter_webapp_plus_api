import 'package:flutter/material.dart';

class ArrestationsScreen extends StatelessWidget {
  const ArrestationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Arrestations')),
      body: const Center(child: Text('ArrestationsScreen (stub)')),
    );
  }
}
