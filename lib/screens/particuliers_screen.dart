import 'package:flutter/material.dart';

class ParticuliersScreen extends StatelessWidget {
  const ParticuliersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Particuliers')),
      body: const Center(child: Text('ParticuliersScreen (stub)')),
    );
  }
}
