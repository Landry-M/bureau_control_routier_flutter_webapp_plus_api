import 'package:flutter/material.dart';

class PermisTemporaireScreen extends StatelessWidget {
  const PermisTemporaireScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Permis/Pl. temporaire')),
      body: const Center(child: Text('PermisTemporaireScreen (stub)')),
    );
  }
}
