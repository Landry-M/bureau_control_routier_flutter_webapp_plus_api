import 'package:flutter/material.dart';

class AvisRechercheScreen extends StatelessWidget {
  const AvisRechercheScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Avis de recherche')),
      body: const Center(child: Text('AvisRechercheScreen (stub)')),
    );
  }
}
